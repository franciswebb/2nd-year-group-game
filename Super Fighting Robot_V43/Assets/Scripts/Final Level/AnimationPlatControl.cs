using UnityEngine;
using System.Collections;

public class AnimationPlatControl : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {
     //   animation.Play("ClipName");
     // set a start time before and a loop of it staying till the next one is there then go
     // if player dies restart as to keep timing

    }

    // Update is called once per frame
    void Update()
    {

    }
}
